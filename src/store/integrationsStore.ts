/**
 * Integrations store — re-exports useIntegrations hook.
 * Connected accounts are persisted in localStorage under 'al_connected_platforms'.
 */
export { useIntegrations } from '@/hooks/useIntegrations';
